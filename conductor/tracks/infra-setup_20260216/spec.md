# Specification: Infrastructure & Core Setup

**Track ID:** infra-setup_20260216
**Type:** Feature
**Created:** 2026-02-16
**Status:** Draft

## Summary
Configuración de la infraestructura base con Docker Compose (MySQL, Redis), inicialización del proyecto NestJS con TypeScript y definición de los Wrappers iniciales para asegurar el agnosticismo de dependencias y el esquema de DB inicial.

## Context
Este track establece los cimientos para el motor **BookingCore-ES**, asegurando que la arquitectura cumpla con los principios de SoC y Agnosticismo de Dependencias definidos en las guías del producto.

## User Story
Como arquitecto, quiero establecer los Wrappers y el boilerplate de NestJS para asegurar que el equipo siga los principios de Clean Architecture desde el día 1.

## Acceptance Criteria
- [ ] Contenedores de MySQL y Redis levantados con Healthchecks funcionales.
- [ ] App NestJS inicializada y respondiendo a un /health check básico.
- [ ] Tablas de inventario y reservas creadas mediante migraciones iniciales.
- [ ] Interfaces (Wrappers) definidos para el repositorio de DB y el cliente de Redis.

## Dependencies
- Ninguna (Proyecto Greenfield).

## Out of Scope
- Lógica de negocio de reservas concurrentes (Track posterior).
- Sistema de pagos real.
- Frontend o UI de administración.

## Technical Notes
- Usar NestJS con módulos (`InventoryModule`, `CommonModule`).
- TypeORM para la persistencia en MySQL.
- Redis para caché (Wrapper inicial).
- Docker Compose para orquestación local.

---

_Generated by Conductor. Review and edit as needed._
